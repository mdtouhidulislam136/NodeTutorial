const sayHi = (name) => {
  console.log(`Hello there ${name}`);
};

// export default

module.exports = sayHi;

const driving = (category) => {
  console.log(`I am dring and incresing speed using ${category}`);
};

module.exports = driving;